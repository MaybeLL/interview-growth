# MVP 本地存储且暂不实现应用级加密

目标数据库只保存在本机，不做自动同步或产品遥测；CLI 仅向当前 Agent 返回完成任务所需的最小 Context Packet，并明确这些内容仍由用户选择的模型提供商处理。数据文件限制为当前用户访问，不保存密钥等秘密，并支持显式导出、备份、恢复和删除。MVP 依赖操作系统磁盘加密，不引入 SQLCipher 与应用密钥恢复，以避免跨平台原生依赖和密钥丢失风险。
