# 模型推理由宿主 Agent 完成

MVP 复用 Codex 宿主执行面试官、评价者和教练角色，Skill 约束其输入与结构化输出，CLI 和应用核心仅校验、持久化、查询和聚合，不持有模型 API Key 或自行调用 LLM。每次生成结果尽可能记录宿主、模型和提示／规约版本。该方案降低了固定评价模型带来的可重复性，但避免额外认证、费用与供应商耦合，并使应用核心保持确定性和易测试；未来可通过独立 Evaluator Provider 扩展。
