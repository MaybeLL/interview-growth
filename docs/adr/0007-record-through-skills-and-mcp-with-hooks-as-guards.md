# Skill 自动记录面试，Hook 只负责守卫与恢复

> 状态：由 [ADR 0015](./0015-use-skills-and-structured-cli-without-mcp.md) 取代。

用户启动面试后，Skill 在正常工作流中自动调用 MCP，依次保存问题、原始作答、评价和追问；用户只负责开始、暂停、结束与纠错，无需逐题发出记录指令。MCP 是唯一写入口，Hook 只检查目标绑定、漏写、失败重试、上下文压缩和中断恢复，不解析任意聊天文本来猜测领域事件。这样比 Hook 自动理解对话更可测试、可移植和可追溯，同时比人工逐题记录更不易遗漏。
