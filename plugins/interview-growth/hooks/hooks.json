{
  "description": "Validate storage and checkpoint active interviews before context compaction.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/src\" uv run --locked --no-editable --project \"${CLAUDE_PLUGIN_ROOT}\" python -m interview_growth.hook_cli healthcheck",
            "timeout": 120,
            "statusMessage": "Checking Interview Growth storage"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/src\" uv run --locked --no-editable --project \"${CLAUDE_PLUGIN_ROOT}\" python -m interview_growth.hook_cli checkpoint",
            "timeout": 120,
            "statusMessage": "Checkpointing active interview"
          }
        ]
      }
    ]
  }
}
